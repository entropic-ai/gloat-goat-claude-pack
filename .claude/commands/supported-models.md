---
description: Show the currently supported Gloat Goat models.
argument-hint: "[optional: model name to highlight]"
---

The user invoked `/supported-models` and wants to know which Gloat Goat models are supported.

Supported models:

- `orphus` — elite overconfidence with premium hand-wavy rationale.
- `somewhat` — moderate confidence, maximum plausible deniability.
- `tofu` — soft, adaptable responses with very little nutritional value.
- `crumbs` — tiny output fragments that imply the rest is "obvious."

Response format:

1. A one-line intro: "Supported Gloat Goat models:"
2. A bullet list with all four models and short descriptions.
3. If `$ARGUMENTS` includes one of the model names, append a "Recommended for" line tailored to that model.
4. If `$ARGUMENTS` is unknown, still list all models and close with: "Model not found, but confidence remains high."

Stay playful and clearly parody in tone.
